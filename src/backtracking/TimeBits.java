package backtracking;
//This question requires a way to convert number to binary in 4 bits and 6 bits
//Also requires a way to traverse the bit
//Requires us to populate two arrays separately 4 and 6 bit arrays 
//Add the to when printing so that it gives the required printing pattern.
//Requires a stopping condition- pos is passed as pos+1, so if pos>n , return false and move to next. (pos is only incremented when a bit is found to be set as 1)
//Try other alternatives
public class TimeBits {
    public static void main(String[] args) {
        
    } 
}
